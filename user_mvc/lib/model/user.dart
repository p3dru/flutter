//Aqui definimos apenas uma classe simples com os atributos id, name e email;
class User {
  final String id;
  final String name;
  final String email;
  //aqui é onde as "construímos"
  User({required this.id, required this.name, required this.email});
}
